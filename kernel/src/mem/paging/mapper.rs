use address_space::{PhysicalAddress,LogicalAddress,PhysicalAddressRange,LogicalAddressRange};

struct mapper{}

impl mapper {
    pub fn map(&mut self, l_addr:  LogicalAddress, p_addr: PhysicalAddress) {
        
    }
}

